name: Test

env:
  CARGO_TERM_COLOR: always

on: pull_request

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: lint
        run: cargo +nightly fmt --check
      - name: unit test
        run: cargo test
